#!/usr/bin/env bash
# Removes the default Nginx configuration file, creates a symbolic link to the default config,
ln -sf /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default
service nginx start
kill "$(pgrep 'nginx' | head -1)"